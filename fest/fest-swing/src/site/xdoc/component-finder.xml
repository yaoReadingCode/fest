<?xml version="1.0" encoding="UTF-8"?>
<document>

  <properties>
    <title>Looking up Components with ComponentFinder</title>
  </properties>

  <body>
    <section name="Looking up Components with ComponentFinder">
      <p>
        Although we recommend to look up GUI components using a 
        <code class="code"><a href="container-fixture.html">ContainerFixture</a></code>, we can also use a 
        <code class="code">ComponentFinder</code>. 
      </p>
      <p>  
        <code class="code">ComponentFinder</code> supports different types of component lookup:
        <ul>
          <li>by name</li>
          <li>by type</li>
          <li>by name <strong>and</strong> type</li>
          <li>custom search criteria using a <code class="code">ComponentMatcher</code></li>
          <li>custom search criteria using a <code class="code">GenericTypeMatcher</code></li>
        </ul>        
      </p>
      <p>
        For more details, please read ComponentFinder's 
        <a href="http://fest.easytesting.org/swing/apidocs/org/fest/swing/core/ComponentFinder.html" target="_blank">Javadoc documentation</a>.
      </p>
      <subsection name="Obtaining a ComponentFinder">
        <p>
          There are three ways to obtain a <code class="code">ComponentFinder</code>:
          <ol>
            <li>
              <p>
                We can create a new <code class="code">ComponentFinder</code> that has access to <strong>all</strong> 
                the GUI components in the AWT hierarchy:
                <pre class="prettyprint">
<span class="typ">ComponentFinder</span> finder = ComponentFinder.<span class="fnc">finderWithCurrentAwtHierarchy</span>();</pre>
              </p>
            </li>
            <li>
              <p>
                We can create a new <code class="code">ComponentFinder</code> that only has access to the GUI components
                created after it.
              </p>
              <p>
                In the following example, 'finder' has access to 'login' but not to 'main'.
                <pre class="prettyprint">
<span class="typ">MainFrame</span> main = <span class="kwd">new</span>&#160;<span class="fnc">MainFrame</span>();
                
<span class="typ">ComponentFinder</span> finder = ComponentFinder.<span class="fnc">finderWithNewAwtHierarchy</span>();

<span class="typ">LoginDialog</span> login = <span class="kwd">new</span>&#160;<span class="fnc">LoginDialog</span>();</pre>
              </p>
            </li>
            <li>
              <p>
                We can reuse the existing <code class="code">ComponentFinder</code> from a <code class="code">RobotFixture</code>.
              </p>
              <p>
                In the following example we are going to use the <code class="code">RobotFixture</code> from a
                <code class="code">FrameFixture</code> 'frame':  
                <pre class="prettyprint">
<span class="typ">FrameFixture</span> frame = <span class="kwd">new</span>&#160;<span class="fnc">FrameFixture</span>(<span class="kwd">new</span>&#160;<span class="fnc">MyFrame</span>());               
<span class="typ">ComponentFinder</span> finder = frame.robot.<span class="fnc">finder</span>();</pre>
              </p>
            </li>
          </ol>
        </p>
      </subsection>
      <subsection name="Links">
        <p>
          <ul>
            <li><a href="component-lookup.html">Component lookup types</a></li>
            <li><a href="lookup-troubleshoot.html">Component lookup troubleshooting</a></li>
          </ul>
        </p>
      </subsection>
    </section>
  </body>
</document>

