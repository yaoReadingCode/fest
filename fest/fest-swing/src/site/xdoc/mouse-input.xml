<?xml version="1.0" encoding="UTF-8"?>
<document>

  <properties>
    <title>Simulating Mouse Input</title>
  </properties>

  <body>
    <section name="Simulating Mouse Input">
      <p>
        All FEST Fixtures can simulate user input using a mouse. This simulation is not limited to send events to
        <code class="code">MouseListener</code>s. FEST provides true mouse input, at the operating system level. For
        example, simulating a user moving the mouse will result in the mouse pointer moving to a specified
        location.
      </p>
      <subsection name="Simple Click">
        <p>
          The method &quot;<code class="code">click()</code>&quot; can simulate a user clicking a GUI component once, 
          using the left mouse button. The following code listing shows how to simulate a user clicking the button with 
          name &quot;connect&quot;:
          <pre class="prettyprint">
frame.<span class="fnc">button</span>(<span class="str">&quot;connect&quot;</span>).<span class="fnc">click</span>();</pre>
        </p>
        <p>
          The following are alternative (and more verbose) ways to click a component once, using the left mouse button:
          <pre class="prettyprint">
<span class="com">// import static org.fest.swing.core.MouseButton.LEFT_BUTTON;</span>
frame.<span class="fnc">button</span>(<span class="str">&quot;connect&quot;</span>).<span class="fnc">click</span>(LEFT_BUTTON); 

<span class="com">// import static org.fest.swing.fixture.MouseClickInfo.leftButton;</span>
frame.<span class="fnc">button</span>(<span class="str">&quot;connect&quot;</span>).<span class="fnc">click</span>(<span class="fnc">leftButton</span>().<span class="fnc">times</span>(1));</pre>
        </p>
      </subsection>
      <subsection name="Right-Click">
        <p>
          The method &quot;<code class="code">rightClick()</code>&quot; can simulate a user clicking a GUI component once, 
          using the right mouse button. The following code listing shows how to simulate a user right-clicking the text 
          field with name &quot;username&quot;:
          <pre class="prettyprint">
frame.<span class="fnc">textBox</span>(<span class="str">&quot;username&quot;</span>).<span class="fnc">rightClick</span>();</pre>
        </p>
        <p>
          The following are alternative (and more verbose) ways to click a component once, using the right mouse button:
          <pre class="prettyprint">
<span class="com">// import static org.fest.swing.core.MouseButton.RIGHT_BUTTON;</span>
frame.<span class="fnc">textBox</span>(<span class="str">&quot;username&quot;</span>).<span class="fnc">click</span>(RIGHT_BUTTON); 

<span class="com">// import static org.fest.swing.fixture.MouseClickInfo.rightButton;</span>
frame.<span class="fnc">textBox</span>(<span class="str">&quot;username&quot;</span>).<span class="fnc">click</span>(<span class="fnc">rightButton</span>().<span class="fnc">times</span>(1));</pre>
        </p>
      </subsection>
      <subsection name="Double-Click">
        <p>
          The method &quot;<code class="code">doubleClick()</code>&quot; can simulate a user double-clicking a GUI 
          component, using the left mouse button. The following code listing shows how to simulate a user 
          double-clicking the label with name &quot;configure&quot;:
          <pre class="prettyprint">
frame.<span class="fnc">label</span>(<span class="str">&quot;configure&quot;</span>).<span class="fnc">doubleClick</span>();</pre>
        </p>
        <p>
          The following is an alternative (and more verbose) way to double-click a component, using the left mouse 
          button:
          <pre class="prettyprint">
<span class="com">// import static org.fest.swing.fixture.MouseClickInfo.leftButton;</span>
frame.<span class="fnc">label</span>(<span class="str">&quot;configure&quot;</span>).<span class="fnc">click</span>(<span class="fnc">leftButton</span>().<span class="fnc">times</span>(2));</pre>
        </p>
      </subsection>
      <subsection name="Click Using Specific Button, Once">
        <p>
          The method &quot;<code class="code">click(MouseButton)</code>&quot; can simulate a user clicking a GUI 
          component, using the specified mouse button. The following code listing shows how to simulate a user 
          clicking the check box with name &quot;include&quot; using the middle mouse button:
          <pre class="prettyprint">
<span class="com">// import static org.fest.swing.core.MouseButton.MIDDLE_BUTTON;</span>
frame.<span class="fnc">checkBox</span>(<span class="str">&quot;include&quot;</span>).<span class="fnc">click</span>(MIDDLE_BUTTON);</pre>
        </p>
      </subsection>
      <subsection name="Click Using Specific Button, Many Times">
        <p>
          The method &quot;<code class="code">click(MouseClickInfo)</code>&quot; can simulate a user clicking a GUI 
          component, using the specified mouse button, many times. The following code listing shows how to simulate a user 
          clicking three times the radio button with name &quot;monthly&quot; using the left mouse button:
          <pre class="prettyprint">
<span class="com">// import static org.fest.swing.fixture.MouseClickInfo.leftButton;</span>
frame.<span class="fnc">radioButton</span>(<span class="str">&quot;monthly&quot;</span>).<span class="fnc">click</span>(<span class="fnc">leftButton</span>().<span class="fnc">times</span>(3));</pre>
        </p>
      </subsection>
    </section>
  </body>
</document>  
